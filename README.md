# flac2mp3

Multi-threaded script to convert flac music library into mp3. Recursively looks for .flac files in the input directory and converts them to .mp3 files in the output directory (preserves directory structure).